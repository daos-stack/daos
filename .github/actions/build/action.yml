name: 'Build Action'
description: 'Build DAOS in a Docker container'
author: 'Brian J. Murrell'
inputs:
  distro:
    description: 'Distro to build'
    default: 'centos7'
runs:
  using: 'docker'
  image: 'utils/docker/Dockerfile.centos7'
  args:
    - ${{ inputs.myInput }}
