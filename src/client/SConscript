"""Build DAOS client"""

def scons():
    """Execute build"""

    SConscript('addons/SConscript')
    SConscript('api/SConscript')
    SConscript('dfs/SConscript')

    # Do not enable the dfuse build yet, it does build however throws
    # compiler warnings on some systems.
    # SConscript('dfuse/SConscript')

if __name__ == "SCons.Script":
    scons()
