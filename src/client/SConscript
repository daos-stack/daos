"""Build DAOS client"""

def scons():
    """Execute build"""

    SConscript('addons/SConscript')
    SConscript('api/SConscript')
    SConscript('dfs/SConscript')

    # Do not enable the dfuse build for now as it still throws warnings.
    SConscript('dfuse/SConscript')

if __name__ == "SCons.Script":
    scons()
