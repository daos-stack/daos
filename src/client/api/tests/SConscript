"""Build client tests"""


def scons():
    """Execute build"""
    Import('denv')

    libs = ['daos', 'daos_common', 'gurt', 'cart', 'pthread', 'cmocka']

    for test in ('eq_tests.c', 'agent_tests.c', 'job_tests.c'):
        denv.Install('$PREFIX/bin', denv.d_test_program(test, LIBS=libs))


if __name__ == "SCons.Script":
    scons()
