#!python

Import('env', 'PREREQS')

libs = ['daos_sr', 'daos_m', 'daos_mgmt', 'daos_common', 'daos_transport',
	'daos_event', 'mpi', 'uuid']

denv = env.Clone()

if PREREQS is not None:
    PREREQS.require(denv, 'ompi')

denv.Program('simple_array', 'simple_array.c', LIBS=libs)
