# Unittests Name and Parameter to be mention in this file.
hosts:
  test_servers:
    - server-A
timeout: 600
server_config:
  name: daos_server
UnitTest:
  agent_tests:
    testname: agent_tests
  job_tests:
    testname: job_tests
