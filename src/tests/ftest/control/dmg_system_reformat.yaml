hosts:
  test_servers: 1
  test_clients: 1
timeout: 300
server_config:
  name: daos_server
  engines_per_host: 1
  engines:
    0:
      storage: auto
pool:
  control_method: dmg
  scm_size: 90%
  nvme_size: 90%
