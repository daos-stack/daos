hosts:
  test_servers: 1
timeout: 140
server_config:
  disable_hugepages: true
  name: daos_server
  engines_per_host: 1
  engines:
    0:
      storage:
        0:
          class: ram
          scm_mount: /mnt/daos
