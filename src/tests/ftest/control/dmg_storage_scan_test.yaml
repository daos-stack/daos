# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - boro-10
server_config:
  name: daos_server
daos_shell:
  hostlist: boro-10:10001
  command: storage
  action: scan
daos_server:
  targets: 1
  config: utils/config/examples/daos_server_sockets.yml
