# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - boro-A
server_config:
  name: daos_server
  port: 10001
  targets: 1
dmg:
  request: storage
  action: scan
  insecure: True
