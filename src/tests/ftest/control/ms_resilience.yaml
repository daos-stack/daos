# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
    - server-B
    - server-C
    - server-D
    - server-E
    - server-F
    - server-G
    - server-H
timeout: 150
pool:
  mode: 146
  scm_size: 1073741824
  control_method: dmg
dmg:
  json: True
