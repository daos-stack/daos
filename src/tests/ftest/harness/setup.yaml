hosts:
  test_servers:
    - server-A
    - server-B
    - server-C
    - server-D
    - server-E
    - server-F
    - server-G
  test_clients:
    - client-B
timeout: 120
server_config:
  name: daos_server
  servers:
    scm_size: 20
    bdev_class: nvme
    bdev_list: ["aaaa:aa:aa.a"]
server_manager:
  storage_prepare_timeout: 60
  storage_format_timeout: 60
