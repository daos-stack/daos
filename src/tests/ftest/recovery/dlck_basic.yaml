hosts:
  test_servers: 1
  test_clients: 1

timeout: 500

server_config:
  name: daos_server
  engines_per_host: 1
  engines:
    0:
      targets: 4
      storage: auto
    1:
      targets: 4
      storage: auto

setup:
  start_servers_once: False

pool:
  size: 50%
