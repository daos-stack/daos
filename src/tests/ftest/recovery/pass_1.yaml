hosts:
  test_servers: 4

timeout: 180

server_config:
  name: daos_server
  engines_per_host: 1
  engines:
    0:
      nr_xs_helpers: 1
      storage: auto

setup:
  start_servers_once: False

pool:
  control_method: dmg
  size: 60G

container:
  control_method: daos
