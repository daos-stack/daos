hosts:
  test_servers:
    - server-A
timeout: 600
server_config:
  name: daos_server
  transport_config:
    allow_insecure: False
agent_config:
  transport_config:
    allow_insecure: False
dmg:
  transport_config:
    allow_insecure: False
