# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - boro-53
timeout: 600
faults:
    no_faults:
      fault_list: []
server_config:
   name: daos_server
poolparams:
   createset:
     setname: daos_server
   createsize:
       size: 107374182
snapshot:
    dkey: "dkey"
    akey: "akey"
    test_datasize: 200
    num_of_snapshot: 20
object_class: !mux
    OC_S1:
        obj_class: 200
