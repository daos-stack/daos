# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
    test_machines1:
      test_machines:
        - boro-35
        - boro-34
timeout: 500
faults:
    no_faults:
      fault_list: []
server_config:
   name: daos_server
poolparams:
   createmode:
     mode: 511
   createset:
     setname: daos_server
   createsize:
       size: 107374182
snapshot:
    dkey: "dkey"
    akey: "akey"
    test_datasize: 200
    num_of_snapshot: 10
object_class: !mux
    OC_S1:
        obj_class: 200
