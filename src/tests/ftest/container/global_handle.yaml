# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
timeout: 60
server_config:
  name: daos_server
pool:
  control_method: dmg
  mode: 511
  name: daos_server
  scm_size: 1073741824
