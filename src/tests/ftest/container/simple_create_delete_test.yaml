# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - boro-53
server_config:
   name: daos_server
pool:
  mode: 511
  name: daos_server
  scm_size: 1073741824
