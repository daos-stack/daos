hosts:
  test_servers:
    - server-A
    - server-B
    - server-C
    - server-D
    - server-E
timeout: 180
pool:
  scm_size: 1G
  control_method: dmg
container:
  control_method: daos
  type: POSIX
