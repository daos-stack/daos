hosts:
  test_servers: 5
timeout: 180
server_config:
  name: daos_server
  servers:
    targets: 4
    nr_xs_helpers: 0
    scm_size: 4
pool:
  scm_size: 1G
  control_method: dmg
container:
  control_method: daos
  type: POSIX
