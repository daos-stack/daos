hosts:
  test_servers:
    - server-A
    - server-B
    - server-C
timeout: 60
server_config:
  name: daos_server
pool:
  control_method: dmg
  name: daos_server
  scm_size: 1G
  mode: 146
container:
  control_method: API
