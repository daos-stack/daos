# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
server_config:
   name: daos_server
timeout: 150
pool:
  control_method: dmg
  mode: 511
  scm_size: 134217728 # 16 mb pool, minimum size currently possible
