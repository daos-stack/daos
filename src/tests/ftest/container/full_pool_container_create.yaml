hosts:
  test_servers: 1
  test_clients: 1
server_config:
   name: daos_server
   servers:
     scm_class: dcpm
     scm_list: ["/dev/pmem0"]
timeout: 900
pool:
  size: 53687091200  # 50G
  threshold_percent: 0.1  # 10%
container:
  control_method: daos
