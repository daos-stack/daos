# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
timeout: 120
hosts:
  test_servers:
    - boro-54
server_config:
    name: daos_server
pool:
    createmode:
        mode: 511
    createset:
        setname: daos_server
    createsize: !mux
        size1gb:
            size: 1073741824
object_class: !mux
    OC_S1:
        obj_class: OC_S1
