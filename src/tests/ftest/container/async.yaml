hosts:
  test_servers: 1
timeout: 240
server_config:
  name: daos_server
  servers:
    targets: 4
    nr_xs_helpers: 0
    scm_size: 4
    log_mask: DEBUG
pool:
  control_method: dmg
  scm_size: 1G
container:
  control_method: API
  env_vars:
    - D_LOG_MASK=DEBUG

