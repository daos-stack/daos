hosts:
  test_servers:
    - server-A
timeout: 240
server_config:
  name: daos_server
  servers:
    targets: 4
    nr_xs_helpers: 0
pool:
  control_method: dmg
  scm_size: 1G
container:
  control_method: API
