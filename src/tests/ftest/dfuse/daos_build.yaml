hosts:
  test_servers:
    - server-A
  test_clients:
    - client-C
timeout: 1500
server_config:
  name: daos_server
pool:
  size: 5GiB
  control_method: dmg
container:
  type: POSIX
  control_method: daos
dfuse:
  mount_dir: "/tmp/daos_dfuse/"
  disable_wb_cache: true
  disable_caching: true
