hosts:
  test_servers:
    - server-A
    - server-B

timeout: 320

server_config:
  servers:
    0:
      targets: 8
      nr_xs_helpers: 0
      scm_size: 4

pool:
  scm_size: 1G
  control_method: dmg
