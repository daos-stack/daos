hosts:
  test_servers:
    - server-A
  test_clients:
    - client-B
timeout: 60
timeouts:
  test_daos_dfs_openat: 10
pool:
  scm_size: 1G
  control_method: dmg
server_config:
  name: daos_server
container:
  type: POSIX
  control_method: daos
daos_tests:
  test_name:
    test_daos_dfuse_openat: DAOS_DFUSE_Openat
  daos_test:
    test_daos_dfuse_openat: false
  num_client:
    test_data_dfuse_openat: true
dfuse:
  mount_dir: "/tmp/daos_dfuse/"
