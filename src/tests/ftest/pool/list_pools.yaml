hosts:
  test_servers:
    - server-A
    - server-B
    - server-C
    - server-D
    - server-E
    - server-F
timeout: 600
server_config:
  name: daos_server
  servers:
    targets: 8
    nr_xs_helpers: 0
    scm_size: 4
pool:
  control_method: dmg
  size: 512M
