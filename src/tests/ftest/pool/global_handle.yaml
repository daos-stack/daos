# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
timeout: 5000
server_config:
  name: daos_server
testparams:
  createmode:
    mode: 511
  createset:
    setname: daos_server
  createsize:
    size: 1073741824
