hosts:
  test_servers: 5
  test_clients: 1

timeout: 300

server_config:
  engines_per_host: 1
  engines:
    0:
      storage: auto

pool:
  size: 10%
  properties: rd_fac:4

container:
  type: POSIX
  cont_rf:
    - 0
    - 1
    - 2
    - 3
    - 4
