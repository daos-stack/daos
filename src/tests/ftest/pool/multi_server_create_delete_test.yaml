# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_machines:
    - boro-35
    - boro-34
server_config:
  name: daos_server
tests:
  users: !mux
    validuser:
      user:
        - valid
        - PASS
    rootuser:
      user:
        - root
        - PASS
    anotheruser:
      user:
        - nfsnobody
        - PASS
  groups: !mux
    validgroup:
      group:
        - valid
        - PASS
    rootgroup:
      group:
        - root
        - PASS
  setnames: !mux
    validsetname:
      setname:
        - daos_server
        - PASS
  tgtlist: !mux
    firsttgt:
      tgt:
        - "0"
        - PASS
##DH++
  pool:
    createset:
      name: daos_server
    createsize:
      scm_size: 16777216

