# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
    - server-B
server_config:
   name: daos_server
timeout: 240
pool:
    scm_size: 1073741824
tests:
   modes:
      modeall:
        mode:
          - 511
          - PASS
   setnames:
      validsetname:
        setname:
          - daos_server
          - PASS
