# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
    - server-B
server_config:
   name: daos_server
timeout: 60
pool:
    name: daos_server
    scm_size: 1073741824
    control_method: dmg
