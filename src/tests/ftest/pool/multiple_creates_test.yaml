# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_machines:
    - boro-A
    - boro-B
server_config:
   name: daos_server
   targets: 1
timeout: 240
tests:
   modes:
      modeall:
        mode:
          - 511
          - PASS
   setnames:
      validsetname:
        setname:
          - daos_server
          - PASS
