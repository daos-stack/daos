# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_machines:
    - boro-35
    - boro-34
server_config:
   name: daos_server
timeout: 240
tests:
   modes:
      modeall:
        mode:
          - 511
          - PASS
   setnames:
      validsetname:
        setname:
          - daos_server
          - PASS
