hosts:
  test_servers:
    - server-A
timeout: 240
server_config:
  name: daos_server
  servers:
    targets: 4
    nr_xs_helpers: 0
    scm_size: 4
pool:
  name: daos_server
  scm_size: 1G
  control_method: dmg
