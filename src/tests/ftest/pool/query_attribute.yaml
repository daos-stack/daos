hosts:
  test_servers:
    - server-A
timeout: 600
server_config:
  name: daos_server
  servers:
    targets: 8
    nr_xs_helpers: 0
    scm_size: 4
pool:
  scm_size: 1000000000
  control_method: dmg
