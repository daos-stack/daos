# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
  test_clients:
    - client-B
timeout: 200
server_config:
  servers_per_host: 2
  name: daos_server
zero_config: !mux
  set:
    env_state: True
  unset:
    env_state: False
