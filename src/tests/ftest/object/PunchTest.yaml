# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
    test_machines1:
      test_machines:
        - boro-A
        - boro-B
timeout: 100
server:
   name: daos_server
pool:
   createmode:
     mode: 511
   createset:
     setname: daos_server
   createsize:
     size: 1073741824
