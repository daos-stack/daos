# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
  test_servers:
    - server-A
timeout: 180
server_config:
   name: daos_server
pool:
   createmode:
     mode: 511
   createset:
     setname: daos_server
   createsize:
     size: 10000000
