# change host names to your reserved nodes, the
# required quantity is indicated by the placeholders
hosts:
   test_machines:
        - boro-A
timeout: 100
server:
   name: daos_server
pool_params:
   createmode:
        mode: 511
   createset:
        setname: daos_server
   createsize:
        size: 1073741824
