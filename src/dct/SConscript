#!python

Import('env')

# generate server module
srv = env.SharedLibrary('daos_ct_srv', Glob('*.c'))
env.Install('$PREFIX/lib/daos_srv', srv)

# generate client library
client = env.SharedLibrary('daos_ct', Glob('*.c'))
env.Install('$PREFIX/lib/', client)

env.AppendUnique(LIBPATH=[Dir('.')])
