#!python

Import('denv')

denv.Program('btree', Glob('btree.c'), LIBS=['daos_common'])
denv.Program('other', Glob('other.c'), LIBS=['daos_common'])
denv.Program('lru', Glob('lru.c'), LIBS=['daos_common'])
