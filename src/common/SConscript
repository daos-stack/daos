#!python

Import('env')
libraries = ['pthread']

common = env.SharedLibrary('libdaos_common', Glob('*.c'))
SConscript('tests/SConscript', exports='env')
env.Append(LIBPATH = ['#/build/common'])
env.Install('#/build/lib/', common)
